{
  "name": "cryptomeme",
  "version": "1.0.0",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "cd server && npm install && npm start"
  },
  "devDependencies": {
    "@shelf/jest-mongodb": "^4.3.2",
    "jest": "^29.7.0"
  }
}
